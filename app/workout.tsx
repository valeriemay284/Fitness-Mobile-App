import { Text, View } from "react-native";

export default function Workout() {
  return (
    <View>
      <Text>Workout Page</Text>
    </View>
  );
}
 //placeholders
 //tutorials + exercises (reccommend based on needs (next semester))